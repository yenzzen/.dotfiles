[global]
    # --- Geometria e Posição ---
    width = 300
    height = 100
    origin = top-right
    offset = 20x20
    notification_limit = 5

    # --- Aparência ---
    font = JetBrainsMono Nerd Font 10
    
    # O SEGREDO DO QUADRADO:
    corner_radius = 0 
    
    # Borda fina e elegante
    frame_width = 2
    frame_color = "{{colors.primary.default.hex}}"
    
    separator_color = frame
    
    # --- Comportamento ---
    padding = 15
    horizontal_padding = 15
    text_icon_padding = 15
    icon_position = left
    min_icon_size = 48
    max_icon_size = 48
    
    # Barra de progresso (ex: volume)
    progress_bar = true
    progress_bar_height = 8
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 300

    # Formatação
    markup = full
    format = "<b>%s</b>%b"
    alignment = left
    vertical_alignment = center
    show_age_threshold = 60
    ellipsize = middle
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes
    
    # Mouse
    mouse_left_click = close_current
    mouse_middle_click = do_action, close_current
    mouse_right_click = close_all

# --- Cores Geradas pelo Matugen ---

[urgency_low]
    background = "{{colors.surface.default.hex}}"
    foreground = "{{colors.on_surface.default.hex}}"
    frame_color = "{{colors.primary.default.hex}}"
    timeout = 4

[urgency_normal]
    background = "{{colors.surface.default.hex}}"
    foreground = "{{colors.on_surface.default.hex}}"
    frame_color = "{{colors.primary.default.hex}}"
    timeout = 6

[urgency_critical]
    # Crítico fica com fundo avermelhado/erro para chamar atenção
    background = "{{colors.error.default.hex}}"
    foreground = "{{colors.on_error.default.hex}}"
    frame_color = "{{colors.on_error.default.hex}}"
    timeout = 0 # Fica na tela até você fechar
